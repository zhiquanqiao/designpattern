意图：定义对象之间的一种一对多的依赖关系，当一个对象的状态发生改变时候，所有一拉与他的对象都得到通知并且自动更新。

主要解决：一个对象的状态改变给其他对象通知的问题，而且要考虑到易用低耦合，并且保持高度协作。

何时解决：一个对象（目标对象）的状态发生改变，所有依赖对象（观察者对象）将会得到通知，进行广播通知。

关键代码：在抽象类里面存放一个ArrayList来存放观察者们。

优点： 观察者和被观察者是抽象耦合的

缺点： 如果一个被观察者对象有很多直接和简介的观察者的化，将所有的观察者通知到将会很浪费时间。