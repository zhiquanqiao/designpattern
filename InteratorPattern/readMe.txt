迭代器模式（Iterator Pattern） 是java 环境中非常常用的世纪末时。这种设计模式用于顺序访问集合对象的元素，不需要知道
集合对象的底层表示方式。

意图：提供一种方法顺序访问一个聚合对象中的各个元素，而有无需暴露该对象的内部表示

主要解决：不同的方式来便利整个对象。

如何解决：把元素之间的游走交给迭代器，而不是角和对象。

关键代码：定义接口：hastNext next.

优点：支持不同方式遍历一个聚合对象， 迭代器简化了聚合类。在同一个聚合类可以有多个遍历